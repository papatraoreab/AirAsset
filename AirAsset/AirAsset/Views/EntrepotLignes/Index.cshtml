@model IEnumerable<AirAsset.Models.EntrepotLigne>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.entrepot)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.entrepot)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.entrepotID }) |
            @Html.ActionLink("Details", "Details", new { id=item.entrepotID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.entrepotID })
        </td>
    </tr>
}

</table>
