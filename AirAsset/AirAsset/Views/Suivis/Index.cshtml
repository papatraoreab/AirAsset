@model IEnumerable<AirAsset.Models.Suivi>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.suivi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.suivi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.suiviID }) |
            @Html.ActionLink("Details", "Details", new { id=item.suiviID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.suiviID })
        </td>
    </tr>
}

</table>
